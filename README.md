SemRes is an desktop application to locally store and edit BabelNet synsets and relationships between tham, as well as adding own, custom synsets.

Requires Maven to build. Build command:

```mvn package```